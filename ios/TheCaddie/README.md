# TheCaddie iOS Shell

This folder contains the first SwiftUI app-shell source files for the native caddie screen.

The Xcode project/target still needs to be generated and verified on macOS with Xcode, then linked against the `TheCaddieDomain` Swift package. The Windows shell used for this scaffold does not have Xcode tooling available.
